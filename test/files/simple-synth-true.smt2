(set-logic HORN)
(declare-fun F (Int) Bool)
(assert (forall ((x Int))
                (=> (> x 100)
                    (F x))))
(assert (forall ((x Int))
                (=> (F x)
                    (> x 0))))
(check-sat)
(get-model)
