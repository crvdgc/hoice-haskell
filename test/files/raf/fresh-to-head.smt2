(set-logic HORN)
(declare-fun F (Int Int) Bool)
(assert (forall ((x Int) (y Int))
                (=> (and (F x (+ y 1)) (> x 100))
                    (F x y))))
(assert (forall ((x Int) (y Int))
                (=> (F x y)
                    (> x 0))))
(check-sat)
(get-model)
